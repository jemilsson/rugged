[package]
name = "rugged-core"
version = "0.1.0"
publish = false
edition = "2021"

[lib]
name = "rugged_core"

[dependencies]

[lints.rust]
unexpected_cfgs = { level = "allow", check-cfg = ["cfg(kani)"] }
